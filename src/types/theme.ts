/**
 * Theme configuration object with CSS class strings
 */
export interface ThemeClasses {
  bg: string;
  text: string;
  border: string;
  shadow: string;
  shadowSm: string;
  cardBg: string;
  mutedText: string;
  badgeBg: string;
  subCardBg: string;
}
